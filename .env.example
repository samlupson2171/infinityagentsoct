# =============================================================================
# INFINITY WEEKENDS - ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains all environment variables needed for the application.
# Copy this file to .env.local and update the values with your actual configuration.
# 
# SECURITY WARNING: Never commit .env.local to version control!
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# =============================================================================
# MongoDB connection string for the application database
# For local development, use: mongodb://localhost:27017/infinity-weekends
# For production, use MongoDB Atlas or your hosted MongoDB instance
MONGODB_URI=mongodb://localhost:27017/infinity-weekends

# =============================================================================
# AUTHENTICATION CONFIGURATION (REQUIRED)
# =============================================================================
# Base URL for your application - used by NextAuth for callbacks and redirects
# For local development: http://localhost:3000
# For production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# Secret key for NextAuth session encryption and JWT signing
# MUST be at least 32 characters long for security
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-key-must-be-at-least-32-characters-long

# =============================================================================
# EMAIL CONFIGURATION (REQUIRED)
# =============================================================================
# SMTP Configuration for sending emails
# Supports Gmail, Microsoft 365, SendGrid SMTP, and any standard SMTP provider

# SMTP server hostname (required)
# Examples:
#   Gmail: smtp.gmail.com
#   Microsoft 365: smtp.office365.com
#   SendGrid: smtp.sendgrid.net
#   Generic: mail.yourdomain.com
SMTP_HOST=smtp.office365.com

# SMTP server port (required)
# Common ports:
#   587 - STARTTLS (recommended for most providers)
#   465 - SSL/TLS
#   25  - Unencrypted (not recommended)
SMTP_PORT=587

# Email address for SMTP authentication (required)
SMTP_USER=your-email@yourdomain.com

# Password or app-specific password for SMTP authentication (required)
# For Gmail: Use an app-specific password if 2FA is enabled
# For Microsoft 365: Use your account password or app password
SMTP_PASS=your-app-password

# Whether to use secure connection (optional, auto-detected based on port)
# true for port 465, false for other ports
# SMTP_SECURE=false

# =============================================================================
# AI CONTENT GENERATION (OPTIONAL)
# =============================================================================
# OpenAI API key for AI content generation features
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Claude API key for AI content generation (alternative to OpenAI)
# Get your API key from https://console.anthropic.com/
CLAUDE_API_KEY=sk-ant-your-claude-api-key-here

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Application environment - affects logging, error handling, and debugging
# Values: development, production, test
NODE_ENV=development

# Public base URL for the application (used in client-side code)
# Usually the same as NEXTAUTH_URL but can be different for CDN setups
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# =============================================================================
# DEVELOPMENT & TESTING (OPTIONAL)
# =============================================================================
# These variables are only needed for specific development or testing scenarios

# Vercel Blob storage token (if using Vercel Blob for file uploads)
# BLOB_READ_WRITE_TOKEN=your-vercel-blob-token-here

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Update all values marked with "your-" with your actual configuration
# 3. Ensure .env.local is in your .gitignore (it should be by default)
# 4. Run the environment checker: node check-env.js
# 5. For detailed setup instructions, see LAUNCH_GUIDE.md
# =============================================================================